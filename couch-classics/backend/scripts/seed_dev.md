# Seed Data

Seed users are created by `pb_migrations/20260609000001_seed_dev_users.js`.

Run:

```bash
just seed-backend
```

Accounts:

- `alice@example.test` / `couchclassics123` with handle `alice`
- `bob@example.test` / `couchclassics123` with handle `bob`
