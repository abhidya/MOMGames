# Theme

The first pass keeps design tokens in `autoload/theme_tokens.gd` so they are
available to every screen. Replace or extend this folder when final original
fonts, icons, and motion rules are ready.
